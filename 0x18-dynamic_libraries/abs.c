#include "main.h"

/**
 *_abs- computes absolute values of an integer
 *@n: The character to print
 *
 *Return: Always 0.
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
